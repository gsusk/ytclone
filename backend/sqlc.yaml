version: "2"
sql:
  - engine: "postgresql"
    queries: "db/queries"
    schema: "db/migrations"
    gen:
      go:
        out: "internal/store"
        emit_interface: true
        emit_pointers_for_null_types: true